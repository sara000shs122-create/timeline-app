import streamlit as st
import numpy as np
import matplotlib.pyplot as plt

# ------------------------
#     إعدادات الصفحة
# ------------------------
st.set_page_config(
    page_title="N-Predict Timeline",
    layout="wide",
)

# ------------------------
#     الهيدر
# ------------------------
st.markdown("""
<div style='text-align:center;'>
    <h1 style='color:#1b263b;'>🔮 N-Predict Timeline</h1>
    <h3 style='color:#415a77;'>نموذج تنبؤي للأحداث الأمنية — النسخة التفاعلية</h3>
</div>
""", unsafe_allow_html=True)

st.write("---")

# ------------------------
#     مدخلات المستخدم
# ------------------------
st.subheader("🔍 أدخل بيانات اليوم الأمنية")

col1, col2, col3 = st.columns(3)

with col1:
    sev = st.slider("متوسط شدة المخاطر", 0.0, 5.0, 2.0)

with col2:
    inc = st.slider("عدد الحوادث الأمنية", 0, 50, 12)

with col3:
    br = st.slider("محاولات الاختراق", 0, 20, 6)

# ------------------------
#     نموذج التنبؤ
# ------------------------
score = sev * 0.45 + inc * 0.06 + br * 0.12
risk = "🔴 مرتفع" if score > 6 else "🟢 منخفض"

st.write("## 🔥 مستوى التهديد المتوقع غدًا:", risk)

# ------------------------
#     الخط الزمني التنبؤي
# ------------------------
st.write("---")
st.subheader("📊 الخط الزمني التنبؤي للأيام السبعة القادمة")

days = np.arange(1, 8)
preds = score + np.random.normal(0, 0.4, 7)

fig, ax = plt.subplots(figsize=(10, 4))
ax.plot(days, preds, marker='o', linewidth=2)
ax.set_xlabel("الأيام القادمة", fontsize=10)
ax.set_ylabel("مستوى التهديد", fontsize=10)
ax.set_title("الخط الزمني التنبؤي", fontsize=12)

st.pyplot(fig)

# ------------------------
#     ملاحظة
# ------------------------
st.info("""
هذه نسخة تجريبية (MVP) لعرض فكرة التنبؤ الأمني.
يمكن تطوير النموذج ليتصل ببيانات حقيقية ويستخدم نماذج ذكاء اصطناعي متقدمة.
""")